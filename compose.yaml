services:
  web:
    build: ./vajbcha
    command: ["python", "app.py"]
    ports:
      - "5000:5000"
    volumes:
      - ./vajbcha/captcha:/app/captcha
      - ./vajbcha/static:/app/static
      - ./vajbcha/app.py:/app/app.py
    environment:
      - FLASK_ENV=development
      - FLASK_DEBUG=1
