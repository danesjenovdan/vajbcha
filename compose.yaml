services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - /app/.venv  # keep the container's venv isolated from the host mount
    environment:
      - FLASK_ENV=development
      - FLASK_DEBUG=1
